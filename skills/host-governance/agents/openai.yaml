interface:
  display_name: "Host Governance"
  short_description: "Query and govern shared host infrastructure"
  default_prompt: "Use $host-governance to query the authoritative shared host context or coordinate the infrastructure changes required by this project."
