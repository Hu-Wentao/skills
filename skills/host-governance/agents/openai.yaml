interface:
  display_name: "Host Governance"
  short_description: "Govern shared hosts through contracted operations"
  default_prompt: "Use $host-governance to resolve the authoritative host contract, inspect safely, and execute only authorized operations."
