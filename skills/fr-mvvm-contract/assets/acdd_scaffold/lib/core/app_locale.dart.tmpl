import 'package:flutter/widgets.dart';
import 'package:fr_mvvm_locale/fr_mvvm_locale.dart';

class AppLocaleViewModel extends FrLocaleViewModel {
  AppLocaleViewModel() : super(initialState: supported.first, all: supported);

  static const supported = [Locale('zh', 'CN'), Locale('en', 'US')];
}
