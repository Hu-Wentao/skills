interface:
  display_name: "Merge Branch Into Current"
  short_description: "Merge a branch into the current branch with a merge commit"
  default_prompt: "Use $merge-branch-into-current to merge a specified branch into the current branch with an explicit merge commit, auto-detect the source branch when possible, resolve conflicts autonomously by default, and abort if either branch is dirty."
