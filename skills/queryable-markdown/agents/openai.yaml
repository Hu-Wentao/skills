interface:
  display_name: "Queryable Markdown"
  short_description: "Create, maintain, edit, and query contracted Markdown"
  default_prompt: "Use $queryable-markdown to query ordinary Markdown read-only, or create, maintain, edit, and query a Markdown document with a persistent mdq contract when I explicitly request writes."
