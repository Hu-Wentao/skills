interface:
  display_name: "Queryable Markdown"
  short_description: "Design, verify, and repair precise Markdown queries"
  default_prompt: "Use $queryable-markdown to design a precise query contract, verify result quality, and safely repair over-broad Markdown queries."
