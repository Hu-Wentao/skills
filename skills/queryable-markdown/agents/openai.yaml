interface:
  display_name: "Queryable Markdown"
  short_description: "Query and safely edit Markdown with compact output"
  default_prompt: "Use $queryable-markdown to retrieve or edit Markdown records through compact scripted mdq workflows."
