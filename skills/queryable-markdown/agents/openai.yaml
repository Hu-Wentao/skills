interface:
  display_name: "Queryable Markdown"
  short_description: "Query Markdown files and collections with persistent contracts"
  default_prompt: "Use $queryable-markdown to query Markdown files or directory collections read-only, or create, maintain, edit, and query a Markdown document with a persistent mdq contract when I explicitly request writes or a governance workflow requires one."
