interface:
  display_name: "Sync Skill Repo Backend"
  short_description: "Run deterministic shared-skill publication"
  default_prompt: "Use $sync-skill-repo explicitly as the hidden backend for a validated shared-skill publish or compatibility sync."
policy:
  allow_implicit_invocation: false
