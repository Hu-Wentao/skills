interface:
  display_name: "Sync Skill Repo"
  short_description: "Revise, publish, and update one skill"
  default_prompt: "Use $sync-skill-repo after revising this local skill to publish it to GitHub, run its named Skills CLI update, and verify its lock-managed installations."
