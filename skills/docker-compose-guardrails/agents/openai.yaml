interface:
  display_name: "Docker Compose Guardrails"
  short_description: "Enforce Compose and host resource boundaries"
  default_prompt: "Use $docker-compose-guardrails to review this deployment’s limits, cgroup protection, and bounded build behavior."
