interface:
  display_name: "Docker Compose Guardrails"
  short_description: "Guard Compose resources and startup builds."
  default_prompt: "Use $docker-compose-guardrails to review this Compose deployment for resource safety and startup-time builds."
