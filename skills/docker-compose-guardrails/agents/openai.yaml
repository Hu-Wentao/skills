interface:
  display_name: "Docker Compose Guardrails"
  short_description: "Guard Compose and host resource guarantees."
  default_prompt: "Use $docker-compose-guardrails to review this deployment's container limits, host-level minimum-resource protection, and bounded build behavior."
