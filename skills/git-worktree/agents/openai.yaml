interface:
  display_name: "Scripted Git Worktree"
  short_description: "Script worktree completion and safe maintenance"
  default_prompt: "Use $git-worktree to script exact-HEAD worktree completion, delivery, and local maintenance safely."
