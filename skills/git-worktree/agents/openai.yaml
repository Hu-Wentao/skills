interface:
  display_name: "Git Worktree & Branch Maintenance"
  short_description: "Safely organize all local branches and worktrees"
  default_prompt: "Use $git-worktree to audit all local development surfaces, preserve detached or dirty work, and classify each branch and worktree for merge, retain, or deletion."
