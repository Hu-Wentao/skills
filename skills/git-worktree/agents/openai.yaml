interface:
  display_name: "Git Worktree & Owner Completion"
  short_description: "Manage worktrees and mark owner tasks complete"
  default_prompt: "Use $git-worktree to discover the current worktree, complete the owned task safely, and mark its final validated commit complete."
