interface:
  display_name: "Git Worktree"
  short_description: "Create, merge, and safely clean up Git worktrees"
  default_prompt: "Use $git-worktree to manage this repository worktree from creation through merge and cleanup."
