interface:
  display_name: "Local Git Worktrees"
  short_description: "Manage local worktree and branch lifecycles"
  default_prompt: "Use $git-worktree to inspect and complete this local worktree task with exact-HEAD safeguards; do not push."
