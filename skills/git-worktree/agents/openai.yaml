interface:
  display_name: "Git Worktree & Branch Maintenance"
  short_description: "Safely automate Git branch and worktree maintenance"
  default_prompt: "Use $git-worktree to audit an exact repository snapshot, classify every branch and worktree, and apply the merge/delete/retain plan under the shared mutation lock while preserving active work."
