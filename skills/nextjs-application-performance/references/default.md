# Default

Classify the request as `design`, `implement`, `review`, `migrate`, or
`monitor`, then resolve that explicit task before changing a
performance-sensitive Next.js application path. If the task remains ambiguous,
inspect the current data or measurement path and ask only for the product
choice that changes pagination, visibility, API compatibility, or the declared
business-completion condition.
