interface:
  display_name: "Reflex 用法"
  short_description: "构建、拆分或重构 Reflex dashboard 与导航壳 UI"
  default_prompt: "Use $reflex-usage to build, split, or refine a Reflex admin dashboard or navigation shell. When bootstrapping a new app, default to `reflex init --template dashboard` unless `customer_data_app` or `api_admin_panel` is a better fit."
